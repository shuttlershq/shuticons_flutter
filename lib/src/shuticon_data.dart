library shuticons;

import "package:flutter/widgets.dart";

class ShutIconsData extends IconData {
  const ShutIconsData(int codePoint)
      : super(codePoint, fontFamily: "Shuticon", fontPackage: "Shuticon");
}
